extends layout

block content
    div(class="container")
        div(class="row")
            div(id="message-box")
                ul(id="messages")
        div(id="input", class="row", background-color="white")
            form
                label(for="chat-input")
                input(class="ten columns", type="text", placeholder="Enter Message Here", id="chat-input")
                button(class="button-primary", type="submit") 
                    i(class="fa fa-arrow-right")

    
